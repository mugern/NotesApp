<Window x:Class="NotesApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotesApp.Views"
        mc:Ignorable="d"
        Title="Приложение для заметок" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <!-- Верхнее меню -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Файл">
                <MenuItem Header="Выход" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="Администрирование" x:Name="AdminMenu" Visibility="Collapsed">
                <MenuItem Header="Управление пользователями" Click="ManageUsers_Click"/>
                <MenuItem Header="Отправить уведомление" Click="SendNotification_Click"/>
            </MenuItem>
            <MenuItem Header="Вид">
                <MenuItem Header="Светлая тема" Click="LightTheme_Click"/>
                <MenuItem Header="Темная тема" Click="DarkTheme_Click"/>
            </MenuItem>
        </Menu>
        
        <!-- Левая панель: папки -->
        <StackPanel Width="200" DockPanel.Dock="Left" Background="#f0f0f0">
            <TextBlock Text="Папки" FontSize="16" FontWeight="Bold" Margin="10,10,10,10"/>
            <Button x:Name="CreateFolderButton" Content="Создать папку" Margin="10,0,10,10" Click="CreateFolder_Click"/>
            <ListBox x:Name="FoldersList" SelectionChanged="FolderSelectionChanged" Margin="10,0,10,10" Height="300"/>
            
            <!-- Trash section -->
            <TextBlock Text="Корзина" FontSize="16" FontWeight="Bold" Margin="10,20,10,10"/>
            <Button x:Name="ViewTrashButton" Content="Просмотр удаленных элементов" Margin="10,0,10,10" Click="ViewTrash_Click"/>
        </StackPanel>
        
        <!-- Основная часть: заметки -->
        <Grid Margin="10">
            <DataGrid x:Name="NotesDataGrid" AutoGenerateColumns="False" SelectionMode="Single" MouseDoubleClick="EditNote_DoubleClick" Margin="0,0,0,50">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Заголовок" Binding="{Binding Title}" Width="200"/>
                    <DataGridTextColumn Header="Папка" Binding="{Binding Folder.Name}" Width="150"/>
                    <DataGridTextColumn Header="Создано" Binding="{Binding CreatedAt, StringFormat=d}" Width="100"/>
                    <DataGridTextColumn Header="Обновлено" Binding="{Binding UpdatedAt, StringFormat=d}" Width="100"/>
                    <DataGridCheckBoxColumn Header="Архив" Binding="{Binding IsArchived}" Width="70"/>
                </DataGrid.Columns>
            </DataGrid>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button x:Name="CreateNoteButton" Content="Создать заметку" Width="100" Height="30" Margin="0,0,10,0" Click="CreateNote_Click"/>
                <Button x:Name="EditNoteButton" Content="Редактировать заметку" Width="100" Height="30" Margin="0,0,10,0" Click="EditNote_Click" IsEnabled="False"/>
                <Button x:Name="DeleteNoteButton" Content="Удалить заметку" Width="100" Height="30" Margin="0,0,10,0" Click="DeleteNote_Click" IsEnabled="False"/>
                <Button x:Name="ArchiveNoteButton" Content="Архивировать заметку" Width="100" Height="30" Click="ArchiveNote_Click" IsEnabled="False"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>