<Window x:Class="NotesApp.Views.TrashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotesApp.Views"
        mc:Ignorable="d"
        Title="Корзина" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Корзина" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <DataGrid Grid.Row="1" x:Name="TrashDataGrid" AutoGenerateColumns="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Заголовок" Binding="{Binding Title}" Width="200"/>
                <DataGridTextColumn Header="Папка" Binding="{Binding Folder.Name}" Width="150"/>
                <DataGridTextColumn Header="Дата удаления" Binding="{Binding DeletedAt, StringFormat=g}" Width="150"/>
                <DataGridTextColumn Header="Дата истечения" Binding="{Binding DeletedExpiresAt, StringFormat=d}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="RestoreButton" Content="Восстановить" Width="80" Height="30" Margin="0,0,10,0" Click="RestoreButton_Click" IsEnabled="False"/>
            <Button x:Name="DeletePermanentlyButton" Content="Удалить навсегда" Width="120" Height="30" Click="DeletePermanentlyButton_Click" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>