<Window x:Class="NotesApp.Views.UserManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotesApp.Views"
        mc:Ignorable="d"
        Title="Управление пользователями" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Управление пользователями" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <DataGrid Grid.Row="1" x:Name="UsersDataGrid" AutoGenerateColumns="False" SelectionMode="Single" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Имя пользователя" Binding="{Binding Username}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridCheckBoxColumn Header="Админ" Binding="{Binding IsAdmin}" Width="80"/>
                <DataGridTextColumn Header="Создан" Binding="{Binding CreatedAt, StringFormat=d}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="MakeAdminButton" Content="Сделать админом" Width="100" Height="30" Margin="0,0,10,0" Click="MakeAdminButton_Click" IsEnabled="False"/>
            <Button x:Name="RemoveAdminButton" Content="Убрать админа" Width="100" Height="30" Margin="0,0,10,0" Click="RemoveAdminButton_Click" IsEnabled="False"/>
            <Button x:Name="DeleteUserButton" Content="Удалить пользователя" Width="100" Height="30" Click="DeleteUserButton_Click" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>